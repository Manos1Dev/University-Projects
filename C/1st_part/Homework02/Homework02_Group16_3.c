#include <stdio.h>

int main(void) {
    float x, y;
    printf("Dwse duo pragmatikous arithmous\n");
    scanf("%f %f", &x, &y);
    double sum = sum + (x + y);
    double plin = plin + (x - y);
    double pollaplasiasmos = pollaplasiasmos + (x * y);
    double diairesh = diairesh + (x / y);
    printf("To apotelesma ths prostheshs einai: %.2f, ths afaireshs: %.2f, tou pollaplasiasmou: %.2f kai to apotelesma ths diaireshs einai: %.2f \n", sum, plin, pollaplasiasmos, diairesh);
    return 0;
}