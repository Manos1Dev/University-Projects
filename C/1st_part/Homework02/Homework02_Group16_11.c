#include <stdio.h>

int main(void) {
    int x, y;
    printf("Dwse duo thetikes times\n");
    scanf("%d%d", &x, &y);
    x = (x - (y * (x / y)));
    printf("To upoloipo einai %d", x);
    return 0;
}