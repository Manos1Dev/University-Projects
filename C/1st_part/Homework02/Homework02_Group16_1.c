#include <stdio.h>

int main(void) {
    int x,y;
    double z;
    printf("Dwse 2 akeraies times\n");
    scanf("%d%d", &x, &y);
    z = (double) x / (double) y;
    printf("To akribes tous phliko einai: %.1f", z);
    return 0;
}