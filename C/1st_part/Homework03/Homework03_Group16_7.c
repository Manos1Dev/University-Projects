#include <stdio.h>

int main(void) {
    int x, y;
    printf("Dwse duo thetikous dipshfious akeraious arithmous\n");
    scanf("%d%d", &x, &y);
    printf("Oi arithmoi eginan %d kai %d", ((((y % 100) % 10) * 100) + ((x % 100) % 100)), ((((y / 100) * 100) + ((((y % 100) / 10) * 10))) + (x / 100)));
    return 0;
}